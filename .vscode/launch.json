{
  "configurations": [
      {
          "name": "Pwntools GDB",
          "type": "node-terminal",
          "postDebugTask": "debugWithGdb",
          "request": "launch",
          "command": "rm -f /tmp/pwntoolscode-dbgcmd; echo \"${file}\" > /tmp/pwntoolscode-prog; exit"
      }
  ]
}